packages.install("readr")
package.install("readr")
library.install("readr")
require(readr)
install.packages("readr")
install.packages("BH")
install.packages("BH")
q()\
q()
f <- function(n=5000000) {
  x <- 1
  while (x < n) {
    x <- x + 1
  }
}
compiler::disassemble(f)
f(10)
f(10)
f(10)
f(10)
f(10)
compiler::disassemble(f)
f(10)
f(10)
compiler::disassemble(f)
f <- function(n=5000000) {
  x <- 1
  while (x < n) {
    a <- (x <- x + 1)
}
}
f(10)
compiler::disassemble(f)
function() {
+ a <- list( 1 ); b <- (a[[1]] <- a); stopifnot(identical(b, list( 1 )))
+ a <- list(x=1); b <- ( a$x  <-  a); stopifnot(identical(b, list(x=1)))
+ }
function() {
 a <- list(x=1); b <- ( a$x  <-  a); stopifnot(identical(b, list(x=1)))
 a <- list( 1 ); b <- (a[[1]] <- a); stopifnot(identical(b, list( 1 )))
}
f()
b <- 2
a <- (b <- 3)
a
b
a <- list(1)
b <- (a[[1]] <- a)
a
b
