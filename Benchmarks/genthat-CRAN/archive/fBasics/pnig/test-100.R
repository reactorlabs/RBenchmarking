library(fBasics)

function_to_run <- function() {
    x <- -2.53814041037894
    mu <- 0
    delta <- 1
    beta <- 0.3
    alpha <- 1
    fBasics:::pnig(q = x, alpha = alpha, beta = beta, delta = delta, mu = mu)
}




