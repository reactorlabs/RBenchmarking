library(fBasics)

function_to_run <- function() {
    q <- -2.27892425468112
    mu <- 0
    delta <- 1
    beta <- 0.3
    alpha <- 1
    fBasics:::.phyp1(q = q, alpha = alpha, beta = beta, delta = delta, mu = mu)
}




