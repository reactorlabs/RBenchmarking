

genthat_extracted_call <- function() {
    x <- -1.42077454148491
    mu <- 0
    lambda <- -0.5
    delta <- 1
    beta <- 0.3
    alpha <- 1
    fBasics:::pgh(q = x, alpha = alpha, beta = beta, delta = delta, mu = mu, lambda = lambda)
}




